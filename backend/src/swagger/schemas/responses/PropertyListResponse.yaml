type: object
properties:
  success:
    type: boolean
    example: true
  statusCode:
    type: integer
    example: 200
  message:
    type: string
    example: "Group properties retrieved"
  data:
    type: object
    properties:
      items:
        type: array
        items:
          $ref: './PropertyListItem.yaml'
