type: object
properties:
  propertyName:
    type: string
    example: "DBPassword"
  valueHtml:
    type: string
    description: "Sanitized HTML"
    example: "<b>secret</b>"
  valueText:
    type: string
    description: "Plain text version"
    example: "secret"
